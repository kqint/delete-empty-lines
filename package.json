{
  "name": "obsidian-delete-empty-lines",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/main.ts --bundle --platform=node --format=cjs --target=es2018 --external:obsidian --outfile=main.js"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "obsidian": "^1.12.3"
  }
}
